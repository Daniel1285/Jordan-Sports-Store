<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"     
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        ResizeMode="NoResize"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Background="Black">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="310"/>
            <ColumnDefinition Width="490"/>
        </Grid.ColumnDefinitions>

        <Border CornerRadius= "10 0 0 0">
            <Border.Background>
                <ImageBrush ImageSource="/6.jpg"/>
            </Border.Background>
        </Border>

        <Grid>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Option" FontSize="60" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="Admin - screen for product settings and orders." FontSize="13" FontWeight="Black" HorizontalAlignment="Center"  Foreground="White" Margin="0,10,0,0" Opacity="0.8"/>
                <TextBlock Text="Client - Customer display screen for orders." FontSize="13"  Foreground="White" FontWeight="Black" HorizontalAlignment="Center" Opacity="0.8" Margin="0,10,0,0"/>
                <TextBlock Text="Trucking - Information about tracking the package." FontSize="13"  Foreground="White" FontWeight="Black" HorizontalAlignment="Center" Opacity="0.8" Margin="0,10,0,0" />
                <TextBlock Text="Login - Login for an existing user." FontSize="13"  Foreground="White" FontWeight="Black" HorizontalAlignment="Center" Opacity="0.8" Margin="0,10,0,0" />
            </StackPanel>
        </Grid>

        <Border CornerRadius="0 10 10 0" Grid.Column="1" Background="Black">
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="AccountTie" Grid.Column="0" Grid.Row="0" Width="50" Height="70" HorizontalAlignment="Right" Foreground="White" Margin="0,130,0,0"/>
                <materialDesign:PackIcon Kind="AccountDetails" Grid.Column="0" Grid.Row="1" Width="50" Height="70" HorizontalAlignment="Right" Foreground="White" Margin="0,195,0,0" />
                <materialDesign:PackIcon Kind="Truck" Grid.Column="0" Grid.Row="2" Width="50" Height="70" HorizontalAlignment="Right" Foreground="White" Margin="0,265,0,0"/>

                <Button Content="Admin" HorizontalAlignment="Center"  Width="150" Height="45" Click="ManagerButton_Click" Grid.Column="1" FontWeight="Bold" FontSize="22" Margin="0,0,0,100" FontFamily="Palatino Linotype" Style="{StaticResource MaterialDesignRaisedButton}"  
                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
                materialDesign:ButtonProgressAssist.Value="-1" Background="#FF6627D8"/>

                <Button Content="Client" HorizontalAlignment="Center"  Width="150" Height="45" Click="ClientButton_Click" Grid.Column="1" FontWeight="Bold" FontSize="22" Margin="0,30,0,0" FontFamily="Palatino Linotype" Style="{StaticResource MaterialDesignRaisedButton}"  
                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
                materialDesign:ButtonProgressAssist.Value="-1" Background="#FF6627D8"/>

                <Button Content="Truckingorder" HorizontalAlignment="Center"  Width="Auto" Height="45" Click="TruckingButton_Click" Grid.Column="1" FontWeight="Bold" FontSize="22" Margin="0,160,0,0" FontFamily="Palatino Linotype" Style="{StaticResource MaterialDesignRaisedButton}"  
                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
                materialDesign:ButtonProgressAssist.Value="-1" Background="#FF6627D8"/>
                
            </Grid>
        </Border>
        <Frame x:Name="mainFram" Grid.ColumnSpan="2" Grid.RowSpan="4" ContentRendered="myFrame_ContentRendered"/>
    </Grid>
</Window>
